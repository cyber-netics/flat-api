version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "arn:aws:ecs:$AWS_DEFAULT_REGION:$AWS_ACCOUNT_ID:task-definition/Flag-Api"
        LoadBalancerInfo:
          ContainerName: "Flag-Api"
          ContainerPort: $CONTAINER_PORT
